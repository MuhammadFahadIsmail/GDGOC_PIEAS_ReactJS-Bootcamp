import React, { useEffect } from 'react'; 
import bookService from '../services/bookService';
// /src/components/BookCard.jsx

 // Default import

const BookCard = () => {
  const { fetchBooks, addBook } = bookService;
  // Now you can use fetchBooks and addBook
};
